version: "3.7"

services:
  app_proxy:
    environment: 
      APP_HOST: pf-deconz_server_1
      APP_PORT: 8080

  server:
    image: deconzcommunity/deconz
    ports:
      - "8080:80"
      - "8443:443"
      - "5900:5900"
    volumes:
      - ${APP_DATA_DIR}/data:/root/.local/share/dresden-elektronik/deCONZ
    restart: on-failure
    network_mode: host
    environment:
      - TZ=Etc/UTC
    devices:
      - "/dev/ttyUSB0:/dev/ttyUSB0"